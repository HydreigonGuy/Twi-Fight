/*
** Twi-Fight
** 
** File description:
** get_attack_size
*/

#include "game.h"

int get_attack_size(int id)
{
    if (id == 1)
        return (20);
    if (id == 2)
        return (50);
    if (id == 3)
        return (50);
    if (id == 4)
        return (20);
}